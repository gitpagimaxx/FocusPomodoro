<Window
    x:Class="FocusPomodoro.CloseChoiceWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="FocusPomodoro">

    <Grid Padding="20,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            FontSize="18"
            FontWeight="SemiBold"
            Text="Fechar FocusPomodoro" />
        <TextBlock
            Grid.Row="1"
            Margin="0,8,0,16"
            TextWrapping="Wrap"
            Text="O timer pode continuar na bandeja do sistema." />

        <StackPanel Grid.Row="2" Spacing="8">
            <Button
                HorizontalAlignment="Stretch"
                Click="OnMinimizeClick"
                Content="Minimizar para bandeja"
                Style="{StaticResource AccentButtonStyle}" />
            <Button
                HorizontalAlignment="Stretch"
                Click="OnExitClick"
                Content="Encerrar aplicativo" />
            <Button
                HorizontalAlignment="Stretch"
                Click="OnCancelClick"
                Content="Cancelar" />
        </StackPanel>
    </Grid>
</Window>
